{% extends "base.html.twig" %}

{% block stylesheets %}
    <link
        rel="stylesheet"
        type="text/css"
        href="{{ asset('assets/css/registration.css')}}"
{% endblock %}
       
{% block body %}

            <body>
    
			<div class="inner">
				<div class="image-holder">
					<img src="{{ asset('assets/images/others/inscription_fond3.png')}}" alt="">
				</div>
				   <form action="{{ path('connexion')}}" method="post">
                   	<h3>Connexion</h3>
                        <div class="form-grouped">
                            <input class="form-control" name="_username" placeholder="Nom d'utilisateur" required type="text">
                        </div>
                        <div class="form-grouped">
                            <input class="form-control" name="_password" placeholder="Mot de passe" required type="password">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-primary btn-no-hover" type="submit">Connexion</button>
                            
                        </div>
                        <input checked id="remember_me" name="_remember_me" type="checkbox"/>
                        <label for="remember_me">Keep me logged in</label>
                         <div>
                            <a  href="{{ path('app_forgotten_password') }}">Mot de passe oublié ?</a>
                        </div>
                    </form>   
                       
			</div>
	
        </body>
        {% endblock %}